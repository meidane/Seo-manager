# نمونه‌ی .env تولیدیِ «Teams» — کپی به /var/www/teams/.env و مقادیر را پر کن.
# هیچ‌وقت .env واقعی را کامیت نکن (در .gitignore هست).

DEBUG=False
# python -c "import secrets;print(secrets.token_urlsafe(50))"
SECRET_KEY=SpTzJ_15ImzQ5xCK-YFuSAeW9CU8i3Le6_aUac_ZIHBimCqguai74xWxy0ZMOU5IdqY

ALLOWED_HOSTS= newpanel.lomin.co
CSRF_TRUSTED_ORIGINS=https:// newpanel.lomin.co

# کلیدِ پایدارِ رمزنگاریِ دسترسی‌ها (بدونِ آن از SECRET_KEY مشتق می‌شود).
# python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())"
FERNET_KEY=

# ── PostgreSQL ──────────────────────────────────────────────
DB_ENGINE=postgres
DB_NAME=teams
DB_USER=teams
DB_PASSWORD=A3TXfJ(#$)XaqPfGT5Y6
DB_HOST=localhost
DB_PORT=5432
